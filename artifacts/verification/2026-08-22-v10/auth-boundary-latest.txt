Lacuna V10 auth boundary verification — 2026-08-22

Stable production: https://lacuna-five.vercel.app

Live password boundary:
- GET /api/session issued a CSRF cookie: true
- POST /api/auth/signin without x-csrf-token: HTTP 403
- POST /api/auth/signin with the issued CSRF cookie/header and invalid credentials: HTTP 401

Live Google start boundary:
- GET /api/auth/google/start: HTTP 302
- redirect uses scope=openid email profile only
- redirect carries code_challenge_method=S256 and a nonce
- redirect response: Cache-Control no-store, private
- OAuth attempt cookie: HttpOnly; Secure; SameSite=Lax; Max-Age=600
- response policy: strict CSP, frame-ancestors 'none', X-Frame-Options DENY,
  Referrer-Policy strict-origin-when-cross-origin, and HSTS preload

Live callback refusal:
- malformed callback state redirects to /signin?google=state
- the attempt cookie is cleared; no token exchange is reached

Candidate verification:
- current candidate full unit suite: 113 files, 2,242/2,242 passed
- queued candidate adds bounded OAuth state/code parameters and a ten-second
  Google token/JWKS deadline; it is not yet production-promoted because the
  Vercel Hobby deployment quota is exhausted

Voice candidate hardening:
- a stalled speech body reader is cancelled when the bounded acquisition signal
  fires after headers have arrived
- the voice path returns `provider_unavailable` instead of remaining busy
- the regression is covered by `tests/unit/voice-browser.test.ts`; not yet
  production-promoted

Connector candidate hardening:
- the default GitHub fetch transport cancels its response body reader when the
  bounded importer signal fires after headers arrive
- the importer returns `github_timeout` rather than remaining busy
- the regression is covered by `tests/unit/connectors-github.test.ts`; not yet
  production-promoted

HydraDB candidate hardening:
- the HydraDB response body reader is cancelled when the caller aborts after
  headers arrive
- the client returns a bounded transport failure rather than remaining busy
- the regression is covered by `tests/unit/client.test.ts`; not yet
  production-promoted

Browser connector candidate hardening:
- connector JSON is read through a bounded response stream
- caller aborts and request deadlines cancel/release a stalled body reader
- the regression is covered by `tests/unit/web-connectors-client.test.ts`; not
  yet production-promoted

Fresh stable smoke rerun:
- `smoke:auth`: 3/3
- `smoke:google`: 16/16
- `smoke:demo`: 31/31
- `smoke:voice`: 7/7, including bounded `audio/mpeg` provider output
