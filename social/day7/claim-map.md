# Day 7 claim map

Every claim the card and the two captions make, and the thing that backs it.
A claim with no artifact behind it does not go in the post.

Recorded 2026-08-19, against the release tagged in `RELEASE_GATE.md`.

## On the card

| Claim on the card | Backed by |
| --- | --- |
| `profile cloud` / `store HydraDB Cloud, database lacuna, collection backend` | [artifacts/cli/session.txt](../../artifacts/cli/session.txt), the recorded output of `lacuna status` under `LACUNA_PROFILE=cloud` |
| moss-index reaches 13 services | [artifacts/proof/proofs.json](../../artifacts/proof/proofs.json), `proofA.walks[1]`, service list of length 13 |
| depth 3 | same file, `cloud.depth` |
| 32 reads, 1702ms | same file, `cloud.reads` and `cloud.ms` |
| through 13 packages | same file, `cloud.through`, length 13 |
| 1 superseded dependency claim not followed | same file, `cloud.ignored` |
| `Meridian migration_window` returns no answer, reason `never_stated` | same file, `proofB.rows[0].envelope` |
| `Bellwether beta_partner` returns Halverd, replaced 2 earlier values | same file, `proofB.rows[1].envelope`, and [artifacts/cli/session.txt](../../artifacts/cli/session.txt) |
| cited from a data planning call, 2025-04-20 | the evidence line in [artifacts/cli/session.txt](../../artifacts/cli/session.txt) |
| web, CLI and MCP returned this identically | same file, `proofB.identical` is true across three clients |
| the mark at the top left | the same five rows `src/cli/mark.ts` prints, character for character, checked by `tests/unit/cli-mark.test.ts` |

The card is rendered by `npx tsx scripts/social-card.ts social/day7` from
`source.html`. Nothing in it is typed to look like output.

## In the captions

| Claim | Backed by |
| --- | --- |
| the landing terminal used to be hand drawn, with an acme workspace, a qwen2.5 model, a CONNECTED dot and a trace id | the diff of `web/src/landing/Cli.tsx` in commit `b6bf1aa` |
| it carried a CONCEPT PREVIEW label | same diff, the removed line |
| a script now runs the real CLI and the page renders the recording | `scripts/capture-cli.ts` and `web/src/landing/cli-session.ts` |
| `status` reported a node on the laptop while answers came from the cloud | commit `7a79e6d`, and the regression test in `tests/unit/cli-status.test.ts` which fails against the old code |
| the two lines quoted from the first recording | the pre-fix `lacuna status` output, which printed `node`, `namespace`, `graph`, `cell` and `read epoch` and had no `profile` or `store` line at all. The caption quotes the first two of those verbatim. It does not quote the post-fix format, which would be a version of the bug that never printed. |
| it loaded its own config instead of going through the seam `ask` uses | the diff of `src/cli/status.ts`, `loadHydraConfig` replaced by `openSource` |
| 13 services, depth 3, one refused edge | `artifacts/proof/proofs.json` |
| the same 13 from the cloud and the self hosted node | same file, `proofA.walks[1].agree` is true |

## What the post does not claim

- It does not say the product is finished. Google sign in is not implemented,
  no connector syncs, and voice is unconfigured.
- It does not give a benchmark number. The retrieval comparison is in
  `docs/BENCHMARKS.md` and the corpus is generated by this project, which the
  README says next to the numbers.
- It does not say the bug was serious for users. It was found before judging
  and the deployed web was always reading the cloud correctly.
- It does not claim a placement or a result in the hackathon.

## Status

Ready for review. Not published. Nothing here posts automatically.
