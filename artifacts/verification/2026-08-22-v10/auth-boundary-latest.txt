Lacuna V10 auth boundary verification — 2026-08-22

Stable production: https://lacuna-five.vercel.app

Live password boundary:
- GET /api/session issued a CSRF cookie: true
- POST /api/auth/signin without x-csrf-token: HTTP 403
- POST /api/auth/signin with the issued CSRF cookie/header and invalid credentials: HTTP 401

Live Google start boundary:
- GET /api/auth/google/start: HTTP 302
- redirect uses scope=openid email profile only
- redirect carries code_challenge_method=S256 and a nonce
- redirect response: Cache-Control no-store, private
- OAuth attempt cookie: HttpOnly; Secure; SameSite=Lax; Max-Age=600
- response policy: strict CSP, frame-ancestors 'none', X-Frame-Options DENY,
  Referrer-Policy strict-origin-when-cross-origin, and HSTS preload

Live callback refusal:
- malformed callback state redirects to /signin?google=state
- the attempt cookie is cleared; no token exchange is reached

Candidate verification:
- focused auth/session/browser tests pass; the DOCX connector suite passes
  84/84 after the bounded XML extraction fix; the full serial suite passes
  2,278/2,278 across 117 files
- commits `b11b471`, `c48d58d`, `1264423`, `356190c`, `5451bf8`, `dc5c343`,
  `35c32cd`, `6feb07a`, `c4f13e8` and `e3c24ab`
  reduce
  duplicate durable account reads, route confirmed password sessions to
  onboarding, persist GitLab observations, align LongMemEval limitations and
  extract validated DOCX XML without a Mammoth ZIP roundtrip, replay browser
  agent runs safely after lost responses, remove the obsolete DOCX parser
  dependency, keep the password sign-in control locked through session
  confirmation and the final route decision, and fail closed when recovery or
  first-run onboarding cannot confirm the signed-in workspace
- the candidate is not yet production-promoted because Vercel rejected a new
  deployment after the free daily deployment quota was exhausted
- current branch preview: `dpl_GL6xgnCfanJr6QssK3HPY4R3TmkH`
  (`https://lacuna-nb60ab902-vaibhav4046s-projects.vercel.app`; preview env has
  no production Hydra/Google/voice credentials)
- latest preview boundary smoke: auth 3/3; Google 5/16, voice 3/7 and demo
  19/31 because those preview-only environment gates are intentionally absent.

Voice candidate hardening:
- a stalled speech body reader is cancelled when the bounded acquisition signal
  fires after headers have arrived
- a stalled JSON voice response reader is cancelled when the bounded acquisition
  signal fires after headers have arrived
- the voice path returns `provider_unavailable` instead of remaining busy
- the regression is covered by `tests/unit/voice-browser.test.ts`; not yet
  production-promoted

Connector candidate hardening:
- the default GitHub fetch transport cancels its response body reader when the
  bounded importer signal fires after headers arrive
- the importer returns `github_timeout` rather than remaining busy
- the regression is covered by `tests/unit/connectors-github.test.ts`; not yet
  production-promoted

HydraDB candidate hardening:
- the HydraDB response body reader is cancelled when the caller aborts after
  headers arrive
- the client returns a bounded transport failure rather than remaining busy
- the regression is covered by `tests/unit/client.test.ts`; not yet
  production-promoted

Browser connector candidate hardening:
- connector JSON is read through a bounded response stream
- caller aborts and request deadlines cancel/release a stalled body reader
- the regression is covered by `tests/unit/web-connectors-client.test.ts`; not
  yet production-promoted

Agents/Work candidate hardening:
- schedule, run-now, cancel and retry actions clear busy state after thrown
  network or session failures
- bounded recovery copy is shown instead of leaving controls frozen
- focused regressions are covered by `tests/unit/agent-actions.test.ts`; not
  yet production-promoted

Shared browser API candidate hardening:
- session, workspace and agent reads/mutations use a bounded JSON response
  reader that cancels/releases on caller abort or request deadline
- the regression is covered by `tests/unit/web-auth-client.test.ts`; not yet
  production-promoted

Private route guard recovery:
- a failed `/api/session` read now renders a bounded checking/error state with a
  visible `Try again` action instead of returning a blank frozen private page
- the regression is covered by `tests/unit/web-product-contracts.test.ts`; not
  yet production-promoted

Fresh stable smoke rerun:
- `smoke:auth`: 3/3
- `smoke:google`: 16/16
- `smoke:demo`: 31/31
- `smoke:voice`: 7/7, including bounded `audio/mpeg` provider output

Read-only signed-session browser check:
- visiting `/signin` with the existing production session redirected to
  `/app/dash` without prompting for credentials
- `/app/agents` rendered both persisted governed roles (`Researcher` and
  `Reviewer`) and the explicit run control
- `/app/work` rendered the daily schedule and five observed runs
- `/app/connectors` rendered all seven implemented workflows as available
- `/app/voice` rendered `AUTHENTICATED · WORKSPACE BOUND`, `READY`, a
  `START LISTENING` control, and typed-command fallback
- the browser reported no console errors or warnings during these route reads

Production voice operation check:
- typed the non-mutating command `open graph` into the authenticated Voice
  route and sent it
- the controller navigated to `/app/graph` and loaded the graph route
- no browser console errors or warnings were recorded

Production connector review check:
- entered the canonical public repository root
  `https://github.com/vaibhav4046/lacuna`
- `REVIEW SNAPSHOT` produced the explicit manual-import review state without
  issuing a network import or changing workspace memory
- the confirmation control remained separate and unpressed

Production runtime scan:
- Vercel grouped runtime errors for the production project returned no errors
  in the selected one-hour window after the connector lifecycle fix

Promotion attempt:
- candidate web build passed (`138` modules; `vite build` succeeded)
- `npx vercel --prod --yes` was rejected again with
  `api-deployments-free-per-day`; the stable alias was not changed

Candidate auth repair:
- password/legacy accounts no longer dead-end after a Google mismatch: an
  authenticated Settings flow now starts a separate state+PKCE+nonce-bound
  Google link attempt
- callback requires the still-valid password session and the same verified
  Google email, re-reads the account at the write boundary, rotates the
  credential epoch, revokes old sessions, and removes the old password and
  recovery hash
- signed-out link starts and mismatched identities fail closed; focused Google
  auth regressions pass in `tests/unit/google-auth-api.test.ts`
- candidate-only until Vercel deployment quota resets

Candidate password sign-in repair:
- the form keeps its busy lock through the confirmed session read and final
  onboarding/dashboard route decision, preventing duplicate submissions during
  a slow session boundary
- the submit handler also rejects rapid re-entry before the busy state update
  is rendered; recovery uses the same guard
- the source contract regression is in
  `tests/unit/web-product-contracts.test.ts`; focused auth tests, typecheck,
  web build and the full serial suite pass; candidate-only until Vercel
  deployment quota resets

Candidate recovery/onboarding repair:
- recovery now keeps its control locked through session confirmation and shows
  a bounded error if the reset cannot produce a confirmed signed-in session
- first-run onboarding now confirms a signed-in session with a non-null
  workspace before opening `/app/dash`, and always releases its transition lock
- source contract regressions are in `tests/unit/web-product-contracts.test.ts`;
  candidate-only until Vercel deployment quota resets

Candidate workflow safety repairs:
- Agents recommendation controls are disabled while a run is busy, preventing
  a second task selection from reusing the in-flight idempotency key; the
  source contract and focused agent tests pass
- GitLab legacy and direct connector anchors now resolve to the real workflow
  review surface; focused connector tests pass
- the latest serial unit suite passes 2,281/2,281 across 117 files, with no
  skips; candidate-only until Vercel deployment quota resets
- Work cancel/retry and schedule dispatch also reject duplicate lifecycle
  mutations before their busy state renders; focused product contracts pass

Candidate GitLab connector:
- bounded public project snapshot workflow added with canonical project roots,
  fixed GitLab API origin, immutable default-branch commit, SHA-1 blob
  integrity checks, UTF-8/binary/secret filtering, request/file/aggregate
  budgets, cancellation, and durable provenance
- catalog, private route, client decoder, review/confirm UI, and focused tests
  are wired; candidate-only until Vercel deployment quota resets
